apiVersion: apps/v1
kind: Deployment
metadata:
  name: techwave-deployment-green
  labels:
    app: techwave
    version: v2
spec:
  replicas: 2
  selector:
    matchLabels:
      app: techwave
      version: v2
  template:
    metadata:
      labels:
        app: techwave
        version: v2
    spec:
      containers:
      - name: techwave-app
        image: yassinelberrak/techwave-app:latest # Tu imagen real
        ports:
        - containerPort: 80